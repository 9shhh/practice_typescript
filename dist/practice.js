"use strict";
var count = 0;
count += 1;
// count += '갑분 문자열';     에러남
var msg = 'hello world';
var done = true;
var number = [1, 2, 3]; // 숫자 배열
var msgs = ['hello', 'world']; // 문자 배열
// msgs.push(1)     에러남
msgs.push('test');
var mightBeUndefined = undefined; // string or undefined 
var nullableNumber = null; // number or null
var color = 'red'; // red, blue, green 중 하나
// color = 'yellow';     에러남
// color = 'orange';     에러남
